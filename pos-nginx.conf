server {
    listen 8050;  # Listen port 8050
    server_name localhost;  # Server IP

    location / {
        root /usr/share/nginx/html/pos-dev/;  # Path to the project
        index index.html index.htm;
        try_files $uri $uri/ /index.html;
    }
}
